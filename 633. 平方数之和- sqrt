class Solution {
    public boolean judgeSquareSum(int c) {
        for(long i = 0; i*i <=c; i++){ // 不能用int
            double j = Math.sqrt(c- i*i);
            if(j == (long)j){
                return true;
            }
        }
        return false;
    }
}